document.querySelectorAll('.game-card').forEach(c=>c.addEventListener('pointermove',e=>{if(innerWidth<760)return;let r=c.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;c.style.transform=`translateY(-7px) perspective(800px) rotateY(${x*3}deg) rotateX(${-y*3}deg)`}));document.querySelectorAll('.game-card').forEach(c=>c.addEventListener('pointerleave',()=>c.style.transform=''));